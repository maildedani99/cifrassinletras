import React from 'react';
import styles from './objective.module.css';

const Objective = () => {

    return(
        <div className={styles.__objective_div}>
                <a className={styles.__objective_number}>567</a>
        </div>
    )
}
export default Objective;